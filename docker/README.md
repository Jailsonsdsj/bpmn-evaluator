# Docker

Contains the `Dockerfile` and `docker-compose.yml` for running the evaluation pipeline in a container.
Use `docker compose up` or `docker compose run pipeline python main.py ...` as documented in CLAUDE.md.
